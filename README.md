# 🇪🇸 Motivational Spanish Mini App

Telegram Mini App для изучения испанского языка с помощью мотивационных фраз.

## ✨ Возможности

- 📚 102 мотивирующие фразы на испанском
- 💾 Сохранение прогресса в аккаунт Telegram
- ⭐ Система избранных фраз
- 📊 Статистика по дням
- 🔄 Синхронизация данных с сервером
- 🎨 Современный тёмный интерфейс
- 🔐 Привязка к аккаунту пользователя Telegram

## 🚀 Быстрый старт

### Требования

- Node.js 14+
- MongoDB (локально или облачно)
- Git
- Telegram Bot Token (от @BotFather)

### Установка локально

1. **Клонируй репозиторий**
   ```bash
   git clone https://github.com/YOUR_USERNAME/motivational-spanish-miniapp.git
   cd motivational-spanish-miniapp
   ```

2. **Установи зависимости**
   ```bash
   npm install
   ```

3. **Создай .env файл из примера**
   ```bash
   cp .env.example .env
   ```

4. **Отредактируй .env**
   - Вставь BOT_TOKEN от @BotFather
   - Настрой DATABASE_URL для MongoDB

5. **Запусти сервер**
   ```bash
   npm start
   # или для разработки
   npm run dev
   ```

6. **Сервер доступен на** `http://localhost:3000`

## 📋 Структура проекта

```
motivational-spanish-miniapp/
├── .gitignore              # Исключения для git
├── .env.example            # Шаблон переменных
├── .env                    # Локальные переменные (не грузим!)
├── package.json            # Зависимости Node.js
├── server.js               # Backend (Express + MongoDB)
├── index.html              # Telegram Mini App (фронтенд)
└── README.md               # Этот файл
```

## ⚙️ Backend API Endpoints

### POST /api/user/progress
Получение прогресса пользователя

**Request:**
```json
{
  "initData": "query_id=...&user=..."
}
```

**Response:**
```json
{
  "success": true,
  "data": {
    "userId": 123456789,
    "favorites": [],
    "currentIndex": 0,
    "studiedToday": 5,
    "totalStudied": 42,
    "username": "username"
  }
}
```

### POST /api/user/sync
Синхронизация всех данных пользователя

**Request:**
```json
{
  "initData": "...",
  "favorites": [...],
  "currentIndex": 5,
  "studiedToday": 10,
  "totalStudied": 50
}
```

### POST /api/user/favorite/add
Добавление фразы в избранное

### POST /api/user/favorite/remove
Удаление фразы из избранного

### GET /api/cards
Получение всех 102 фраз

### POST /api/user/stats
Получение статистики пользователя

### GET /health
Проверка статуса сервера

## 🔗 Подключение Telegram Mini App

### 1. Создай бота
- Напиши @BotFather в Telegram
- Команда `/newbot`
- Сохрани токен

### 2. Установи WebApp URL
Используй один из вариантов:

**Вариант А: Через curl**
```bash
curl -X POST https://api.telegram.org/bot{YOUR_BOT_TOKEN}/setWebAppInfo \
  -d "button_text=🇪🇸 Испанский" \
  -d "web_app_url=https://your-domain.com/index.html"
```

**Вариант B: Через Bot API в Python**
```python
import requests

TOKEN = "YOUR_BOT_TOKEN"
url = f"https://api.telegram.org/bot{TOKEN}/setWebAppInfo"
data = {
    "button_text": "🇪🇸 Испанский",
    "web_app_url": "https://your-domain.com/index.html"
}
requests.post(url, json=data)
```

## 📦 Развёртывание

### На Vercel (рекомендуется)
```bash
npm install -g vercel
vercel
```

### На Railway.app
1. Подключи GitHub репо
2. Добавь переменные окружения
3. Автоматический деплой

### На Heroku
```bash
npm install -g heroku
heroku login
heroku create your-app-name
git push heroku main
```

### На собственном сервере
```bash
git clone <repo>
cd motivational-spanish-miniapp
npm install
npm start
```

## 🗄️ MongoDB

### Локально
```bash
mongod --dbpath C:\data\db
```

### На облаке (рекомендуется)
1. Зарегистрируйся на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создай кластер (Free Tier)
3. Скопируй connection string
4. Вставь в .env как DATABASE_URL

## 🔐 Безопасность

⚠️ **ВАЖНО:**
- Никогда не коммитим `.env` файлы
- `.gitignore` исключает конфиденциальные данные
- Все токены хранятся в переменных окружения
- Проверяем Telegram initData на сервере

## 📊 Как это работает

1. Пользователь открывает Mini App в Telegram
2. Приложение отправляет `initData` на сервер
3. Сервер проверяет Telegram ID пользователя
4. Загружается профиль из MongoDB
5. Все изменения синхронизируются с БД каждые 30 сек

## 🐛 Отладка

**Сервер не запускается?**
```bash
# Проверь PORT
lsof -i :3000

# Проверь MongoDB
mongosh
```

**API не отвечает?**
```bash
curl http://localhost:3000/health
```

**Данные не сохраняются?**
- Проверь .env переменные
- Проверь подключение к MongoDB
- Посмотри логи в консоли

## 📝 Лицензия

MIT License - используй свободно!

## 👥 Поддержка

Если есть вопросы или баги, создай Issue на GitHub.

---

**Используй это приложение для эффективного изучения испанского! 🚀**

Made with ❤️ for Spanish learners
