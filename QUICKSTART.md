# 📋 БЫСТРЫЙ СТАРТ — СКОПИРУЙ И ВСТАВЬ КОМАНДЫ

## 1️⃣ ПЕРВЫЙ РАЗ — УСТАНОВКА

```powershell
# Откройте PowerShell в папке проекта

# Установи зависимости
npm install

# Запусти сервер
npm start
```

Сервер запустится на `http://localhost:3000`

---

## 2️⃣ РАЗРАБОТКА (с автопереезагрузкой)

```powershell
npm run dev
```

---

## 3️⃣ ЗАГРУЗКА НА GITHUB

### Быстрый способ (автоматический):
```powershell
.\deploy.ps1
```

### Или вручную:
```powershell
git init
git add .
git commit -m "Initial: Telegram Mini App"
git remote add origin https://github.com/YOUR_USERNAME/motivational-spanish-miniapp.git
git branch -M main
git push -u origin main
```

---

## 4️⃣ РАЗВЁРТЫВАНИЕ НА VERCEL

```bash
# Установи Vercel CLI
npm install -g vercel

# Вход
vercel login

# Развёртывание
vercel --prod
```

Или через Web интерфейс на https://vercel.com

---

## 5️⃣ ПРОВЕРКА API

```powershell
# Проверь что сервер работает
curl http://localhost:3000/health

# Результат должен быть:
# {"status":"OK","timestamp":"2025-12-07T..."}
```

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ

| Команда | Что делает |
|---------|-----------|
| `npm start` | Запуск production сервера |
| `npm run dev` | Запуск с автопереезагрузкой |
| `npm install` | Установка зависимостей |
| `npm update` | Обновление зависимостей |
| `git status` | Статус репозитория |
| `git log` | История коммитов |
| `git push` | Загрузка на GitHub |
| `node server.js` | Прямой запуск сервера |

---

## ⚠️ ЕСЛИ ОШИБКИ

### "Порт 3000 уже в использовании"
```powershell
# Найди процесс
Get-NetTCPConnection -LocalPort 3000 | Get-Process

# Или используй другой порт в .env
API_PORT=3001
```

### "MongoDB не подключается"
```powershell
# Проверь строку в .env
# Запусти локально:
mongod --dbpath C:\data\db

# Или используй MongoDB Atlas
```

### "Git не найден"
```powershell
# Установи Git с https://git-scm.com
# Перезагрузи PowerShell
```

---

## 📱 ТЕСТИРОВАНИЕ В TELEGRAM

1. Создай бота через [@BotFather](https://t.me/botfather)
2. Получи токен
3. Замени в `.env`: `BOT_TOKEN=твой_токен`
4. Запусти сервер: `npm start`
5. Развёрнь на хостинге
6. В BotFather установи WebApp URL
7. Откройи бота и нажми на кнопку

---

## 📚 ДОКУМЕНТАЦИЯ

- **README.md** — полное описание проекта
- **DEPLOY.md** — подробная инструкция развёртывания
- **server.js** — комментированный код backend
- **index.html** — комментированный код frontend

---

## 🚀 СКОРОСТНАЯ СХЕМА

```
1. Скачиваешь файлы
     ↓
2. npm install
     ↓
3. npm start (проверка локально)
     ↓
4. git push (на GitHub)
     ↓
5. Развёртывание на Vercel
     ↓
6. Подключение к Telegram Bot
     ↓
✅ ГОТОВО!
```

---

**Все вопросы смотри в README.md и DEPLOY.md! 🎓**
