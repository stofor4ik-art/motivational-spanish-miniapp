# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО РАЗВЁРТЫВАНИЮ

## 📦 Что ты получишь

Это **полный проект** Telegram Mini App с:
- ✅ Frontend (index.html) — интерактивное приложение с 102 фразами
- ✅ Backend (server.js) — Node.js API с MongoDB
- ✅ Синхронизация данных — автосохранение прогресса
- ✅ Привязка к аккаунту TG — каждый пользователь видит свои данные
- ✅ Избранные фразы — асинхронное добавление/удаление

---

## 🎯 ЭТАП 1: Подготовка на локальной машине

### Шаг 1: Скачай все файлы

Создай новую папку и распаковай туда все файлы:
```
motivational-spanish-miniapp/
├── .gitignore
├── .env
├── .env.example
├── package.json
├── server.js
├── index.html
├── README.md
├── deploy.ps1
```

### Шаг 2: Установи Node.js (если нет)

[Скачай Node.js](https://nodejs.org/) версию 14+ (LTS рекомендуется)

Проверь установку:
```powershell
node --version
npm --version
```

### Шаг 3: Установи зависимости

Открой PowerShell в папке проекта и выполни:
```powershell
npm install
```

Должны установиться:
- express (веб-фреймворк)
- mongodb (драйвер БД)
- cors (кросс-доменные запросы)
- dotenv (переменные окружения)
- nodemon (автоперезагрузка при разработке)

### Шаг 4: Настрой .env файл

Отредактируй файл `.env`:

```env
BOT_TOKEN=8059103322:AAHf8ql_pq1FsQEhL3Xd6Dodku4DkCMIu2Y
API_PORT=3000
DATABASE_URL=mongodb+srv://user:password@cluster.mongodb.net/motivational_spanish
NODE_ENV=development
WEBHOOK_URL=https://your-domain.com/webhook
```

⚠️ **Важно**: `BOT_TOKEN` — это тестовый токен. При развёртывании замени на свой!

---

## 🗄️ ЭТАП 2: Настройка MongoDB

### Вариант A: MongoDB Atlas (облако — рекомендуется)

1. Зайди на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создай аккаунт (free tier доступен)
3. Создай новый проект
4. Создай кластер (Free Tier)
5. Создай юзера БД (например: `admin` / `password123`)
6. Получи Connection String вида:
   ```
   mongodb+srv://admin:password123@cluster0.xxxxx.mongodb.net/motivational_spanish?retryWrites=true&w=majority
   ```
7. Вставь это в `.env` как `DATABASE_URL`

### Вариант B: MongoDB локально

1. [Скачай MongoDB Community](https://www.mongodb.com/try/download/community)
2. Установи
3. Запусти service:
   ```powershell
   net start MongoDB
   ```
4. В `.env` установи:
   ```
   DATABASE_URL=mongodb://localhost:27017/motivational_spanish
   ```

---

## ▶️ ЭТАП 3: Запуск локально

### Запуск backend сервера

```powershell
npm start
```

Должно появиться:
```
╔════════════════════════════════════════╗
║  Motivational Spanish Mini App Server  ║
║  Server running on port 3000           ║
║  Environment: development              ║
║  API: http://localhost:3000            ║
╚════════════════════════════════════════╝
```

### Проверка API

Открой браузер и перейди на:
```
http://localhost:3000/health
```

Должно вернуться:
```json
{"status":"OK","timestamp":"2025-12-07T..."}
```

---

## 📤 ЭТАП 4: Загрузка на GitHub

### Способ 1: Автоматический (PowerShell скрипт)

```powershell
.\deploy.ps1
```

Скрипт попросит GitHub username и загрузит всё автоматически.

### Способ 2: Ручной

```powershell
# Инициализация
git init
git config user.email "your@email.com"
git config user.name "Your Name"

# Добавление файлов
git add .

# Проверка (должно исключить .env)
git status

# Коммит
git commit -m "Initial commit: Telegram Mini App - Motivational Spanish"

# Добавление удалённого репо
git remote add origin https://github.com/YOUR_USERNAME/motivational-spanish-miniapp.git

# Загрузка
git branch -M main
git push -u origin main
```

---

## 🌐 ЭТАП 5: Развёртывание на хостинге

### Вариант 1: Vercel (самый простой)

1. Зайди на [Vercel](https://vercel.com)
2. Подключи GitHub (авторизируйся)
3. Импортируй репозиторий
4. Добавь переменные окружения:
   - `BOT_TOKEN` = твой токен
   - `DATABASE_URL` = MongoDB URL
   - `NODE_ENV` = production
5. Deploy!

**Frontend будет на**: `https://motivational-spanish-miniapp.vercel.app`  
**API будет на**: `https://api.motivational-spanish-miniapp.vercel.app` (если настроишь)

### Вариант 2: Railway.app

1. Зайди на [Railway](https://railway.app)
2. Новый проект → Deploy from GitHub
3. Выбери репозиторий
4. Добавь переменные окружения
5. Railway автоматически запустит `npm start`

### Вариант 3: Heroku (скоро платный, но всё ещё работает)

```powershell
# Установка Heroku CLI
npm install -g heroku

# Вход в Heroku
heroku login

# Создание приложения
heroku create motivational-spanish-app

# Добавление переменных
heroku config:set BOT_TOKEN=your_token
heroku config:set DATABASE_URL=your_mongodb_url

# Загрузка
git push heroku main
```

---

## 🤖 ЭТАП 6: Подключение к Telegram

### Способ 1: Через BotFather

1. Открой Telegram и найди [@BotFather](https://t.me/botfather)
2. Команда `/mybots`
3. Выбери свой бот
4. Нажми на кнопку для вебapps
5. Задай WebApp URL:
   ```
   https://your-vercel-domain.vercel.app/index.html
   ```

### Способ 2: Через API (curl)

```powershell
$TOKEN = "8059103322:AAHf8ql_pq1FsQEhL3Xd6Dodku4DkCMIu2Y"
$URL = "https://your-vercel-domain.vercel.app/index.html"

curl -X POST "https://api.telegram.org/bot$TOKEN/setWebAppInfo" `
  -d "button_text=🇪🇸 Испанский" `
  -d "web_app_url=$URL"
```

---

## ✅ ПРОВЕРКА ЧТО ВСЁ РАБОТАЕТ

1. **Откомпилируй бота** в Telegram
2. **Нажми на кнопку** WebApp
3. **Должно загрузиться** приложение
4. **Проверь функции**:
   - [ ] Переход между фразами
   - [ ] Скрытие/показ перевода
   - [ ] Добавление в избранное
   - [ ] Клик на избранное открывает фразу
   - [ ] При перезагрузке остаются данные (синхронизация)

---

## 🐛 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ

### API не отвечает
```powershell
# Проверь что сервер запущен
npm start

# Проверь порт
lsof -i :3000

# Если порт занят
Get-Process -Id (Get-NetTCPConnection -LocalPort 3000).OwningProcess | Stop-Process
```

### MongoDB не подключается
```powershell
# Проверь строку подключения в .env
# Убедись что база существует
# Проверь пароль

# Или используй локальный MongoDB
mongod --dbpath C:\data\db
```

### Telegram WebApp не загружается
- Проверь URL в BotFather верный
- Проверь что index.html на хостинге доступен
- Открой DevTools (F12) и посмотри ошибки

### Данные не сохраняются
- Проверь что backend запущен
- Посмотри логи в консоли
- Убедись что .env переменные установлены правильно

---

## 📊 ПОЛЕЗНЫЕ КОМАНДЫ

```powershell
# Запуск с автоперезагрузкой (разработка)
npm run dev

# Проверка node_modules
npm ls

# Обновление зависимостей
npm update

# Проверка версии Node
node -v

# Удаление node_modules и переустановка
Remove-Item node_modules -Recurse
npm install

# Просмотр логов MongoDB
Get-EventLog -LogName "MongoDB"
```

---

## 🎓 ЧТО ДАЛЬШЕ?

1. **Добавь свои фразы** в массив `cards` в index.html и server.js
2. **Улучши статистику** — добавь больше метрик
3. **Создай admin панель** — для управления фразами
4. **Добавь соревнования** — мульти-плеер режим
5. **Интеграция с TG каналом** — делись результатами

---

## 📞 ПОДДЕРЖКА

Если что-то не работает:
1. Посмотри `README.md` в проекте
2. Проверь консоль (F12 → Console)
3. Посмотри логи backend (npm start)
4. Проверь переменные окружения

---

**Удачи с развёртыванием! 🚀**
